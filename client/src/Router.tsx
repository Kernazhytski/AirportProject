import React from 'react';
import {Route} from "react-router-dom";

const Router = () => {
    return (
        <div>
            <Route path={'*'}>

            </Route>
        </div>
    );
};

export default Router;